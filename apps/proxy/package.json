{
  "name": "@coreframe-relay/proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Coreframe Relay inbound webhook proxy — Hono on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "@coreframe-relay/types": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0"
  }
}
