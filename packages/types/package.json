{
  "name": "@coreframe-relay/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod schemas and inferred types. The single source of truth the dashboard and the proxy both compile against.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./internal": "./src/internal.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
