{
  "name": "coreframe-relay",
  "private": true,
  "version": "0.1.0",
  "description": "Coreframe Relay — webhook infrastructure. Buffer (MVP), Gate (phase 2), Shield (phase 3).",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "test:security": "turbo run test:security"
  },
  "devDependencies": {
    "supabase": "^2.111.0",
    "turbo": "^2.3.3"
  }
}
