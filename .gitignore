# Dependencies
node_modules/
.pnpm-store/

# Builds
.next/
out/
dist/
.turbo/
*.tsbuildinfo

# Secrets — .env.example is committed, real values never are.
.env
.env.*
!.env.example

# Cloudflare / wrangler
.wrangler/
.dev.vars

# Supabase local state
supabase/.branches/
supabase/.temp/

# Test output
coverage/
playwright-report/
test-results/

# Editor / OS
.DS_Store
*.log

# graphify knowledge graph: local-only token/cost ledger.
# graphify-out/ itself is intentionally NOT ignored - it is meant to be committed.
graphify-out/cost.json

# graphify generated index — rebuilt by `graphify update .`, never hand-edited.
graphify-out/

# git worktrees (parallel agent branches) - not repo content.
.kilo-worktrees/
.worktrees/
